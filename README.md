# desktop-scrollery
A KWin script to make your desktops feel more spacious by allowing you to scroll them

# Building
All you really need to do is zip `metadata.json` and `contents` as `desktop-scrollery.kwinscript`. I've provided a build script to do it for you, run `build.sh`.